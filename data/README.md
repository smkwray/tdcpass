# Data directories

- `raw/` — direct downloads or copied artifacts
- `cache/` — temporary or shared caches
- `derived/` — reproducible pipeline outputs
- `examples/` — synthetic example data
- `site/` — site-ready exported artifacts

Core rule:
raw and cache data can be recreated; derived data should be generated by code; examples are safe to track.
